
  # Fire and Smoke Detection Interface

  This is a code bundle for Fire and Smoke Detection Interface. The original project is available at https://www.figma.com/design/BlNxEqfJ2HrQ6a9DBKat48/Fire-and-Smoke-Detection-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  